# Implement feature

1. Read `_contract/` relevant files and the active `plans/NNN-*.md`.
2. Implement only what the plan specifies.
3. Use existing `components/ui` and `lib/flux` patterns.
4. Run `pnpm test` and `pnpm check:drift`.
5. Check off plan items.
