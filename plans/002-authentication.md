# Plan 002 — Authentication

## Checklist

- [x] Auth.js v5 with env-gated GitHub + Google
- [x] `profiles` migrations + RLS + grants + api schema template
- [x] Protected `(dashboard)` layout
- [x] Login page + profile settings
- [x] Profile server actions via `fluxJson`

## Exit

Sign in with configured provider; profile upsert works against Flux.
